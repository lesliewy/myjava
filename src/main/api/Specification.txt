1,框架就是一个类库的集合。集合框架就是一个用来表示操作集合的统一的框架，包含了实现集合的接口和类.

             Collection(I)                        MAP(I)
             /        \                            |
            /          \                           |
         Set(I)       List(I)                   SortedMap(I)
         /
        /
    SortedSet(I)
    
    Collection: 存放一个个对象，每次存放单个对象.
    
 主要实现类：
         |- HashSet                          |- ArrayList
    Set -|                             List -|
         |- LinkedHashSet                    |- LinkedList
         
    
    Map  -HashMap
    SortedMap - TreeMap