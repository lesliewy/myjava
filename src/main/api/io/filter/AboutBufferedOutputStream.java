package api.io.filter;

public class AboutBufferedOutputStream {
	/*
	 * 1,BufferedOutputStream(OutputStream out);
	 *   BufferedOutputStream(OutputStream out,int size);  // 按指定大小创建输出缓冲区.
	 */

}
